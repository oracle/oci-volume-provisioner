# Storage class to create mount target on without any additional information
# Assumes a mount target has already been created and a random one shall be chosen
kind: StorageClass
apiVersion: storage.k8s.io/v1beta1
metadata:
  name: oci-fss
provisioner: oracle.com/oci-fss